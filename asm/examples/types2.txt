# or a double pointer because of array-pointer equivalence
ia strs 6 "this" "is" "an" "array" "of" "strings"

ba bytes 3 0 1 2
fa floats 3 1. 2. 3.

out_f @floats

i i
i addr
i val

load_i 0
store i
lbl l

    load strs
    add @i
    store addr
    deref @addr
    store val

    out_s @val
    out_c 'a'
    out_c '\n'

load @i
add_i 4
store i
cmp_i 24
ifle
jmp l
flush
halt

halt
